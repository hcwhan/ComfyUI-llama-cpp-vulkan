# 已核实无问题的关键结论

> 全面 code review 复核与修复过程中确立的结论存档,
> 避免将来重复排查或重新争论。所有条目均对照实际源码
> (本机安装的 llama-cpp-python Vulkan wheel、ComfyUI 主程序、llama.cpp 上游、
> Impact Pack 生态) 逐一核实。

## Vulkan / 设备管理

- Vulkan 纯度: 全部 app/ 源码无 CUDA/ROCm/HIP 假设; 与 torch 的唯一耦合点是加载前
  `mm.free_memory` 腾挪显存 (签名与 ComfyUI 0.27.0 匹配), 设备枚举/选卡走 ggml C API 完全独立.
- 选卡语义: 显式选卡传 `SPLIT_MODE_NONE + main_gpu 索引` 与上游 `llama_prepare_model_devices`
  的 "NONE 时以 main_gpu 为 model->devices 下标取单卡" 逻辑吻合 (对照上游源码确认).
- 核显收集规则: llama.cpp 上游的 IGPU 分支只收第一个核显 (`if (igpus.empty())` 才 push),
  且仅在无独显时加入; 插件 `_selectable_devices()` 的 `[:1]` 与 docstring、AGENTS.md 表述
  均正确. wheel vendored 的 llama.cpp 无法直查源码, 若多核显系统实测异常再回头复核.

## Chat handler / thinking

- `_HANDLER_SPECS` 对 wheel `llama_multimodal` 的覆盖完整 (全部 24 个 handler 类均被引用,
  含借给 DeepSeek-OCR 的基类 MTMDChatHandler); 注册表为 "显示名 -> (类名, 构造 kwargs)"
  形态, kwargs 合法性与 -Thinking 后缀一致性由 tests/test_handlers.py 契约测试锁定.
- Gemma4 的思考块格式是 `<|channel>thought ... <channel|>` 而非 `<think>`,
  `strip_thinking_blocks()` 的正则对它不适用; 其 enable_thinking 仅 31B/26BA4B 支持
  (wheel docstring), 参数只影响模板渲染: True 注入 `<|think|>` 系统块,
  False 向 generation prompt 注入预闭合空思考通道.
- GLM-4.1V 的输出形态为 `<think>...</think><answer>正文</answer>` (官方推理代码按
  `<answer>(.*?)</answer>` 提取正文); wheel handler 以 `</answer>` 为 stop token,
  闭合标签不进入文本, 因此剥掉 think 块后正文以 `<answer>` 开头, 已在 strip 层剥离
  并有单测覆盖. 官方推理代码与模型文档的核实依据已足够, 无需本地 GGUF 实测.
  GLM-4.6V 不受影响 (stop 列表无 `</answer>`, 模板不用 answer 包裹).
- extract_text 中 `.removeprefix(": ")` 针对 Vicuna/LLaVA 风格模板 (generation prompt 以
  "ASSISTANT:" 收尾, 如 Llava15ChatHandler 与部分 GGUF 内嵌模板), 部分模型会把冒号
  连同空格再输出一遍.

## ComfyUI 侧机制

- 结果缓存: 节点缓存签名 = 类名 + IS_CHANGED 结果 + 全部祖先签名 + 输入常量;
  `IS_CHANGED` 在每次排队开始时统一求值, 且刻意只传常量 (连线输入不解析),
  实现须用 `**kwargs` 兜底签名. `NOT_IDEMPOTENT` 只把 node_id 掺入签名
  (解决同图同参节点去重), 不影响跨排队缓存.
- Unload Model 节点维持缓存语义 (上游输出未变化时不重复卸载) 是有意决策,
  已在 docstring/tooltip 说明; 若未来要强制执行, 最优解是条件 `IS_CHANGED`
  (`LLAMA_CPP_STORAGE.llm is not None` 时返回 NaN, 否则稳定值),
  既修复跨工作流场景又不产生常态化下游重算.
- `InterruptProcessingException` 为 BaseException 子类 (查 ComfyUI 源码确认),
  插件 `except Exception` 不会误吞中断; InterruptWatcher 持续重复 set 的抗竞态设计
  与 wheel 每次请求开始 `_abort_event.clear()` 匹配.
- 列表语义声明组合 (`json_to_bboxes` INPUT_IS_LIST+OUTPUT_IS_LIST、`bboxes_to_bbox`
  INPUT_IS_LIST、`split_instruct_output` OUTPUT_IS_LIST 等) 正确, widget `[0]` 解包无遗漏.

## 下游生态兼容

- Impact Pack 的 SEG bbox dtype: 其生态内 float32 ndarray 与整型 tuple 两种形态并存
  (最主要的 SEGS 生产者 UltraBBoxDetector 把 YOLO xyxy 的 float32 ndarray 原样透传,
  `mask_to_segs` 则产整型 tuple), 下游节点本须兼容 float;
  本插件 `bboxes_to_segs` 写 float32 与检测器路径的惯例一致, 无需改整型.

## llama-cpp-python wheel 契约

- `llama_cpp_parameters` 12 个字段名与 `create_chat_completion` 签名对应
  (含 JamePeng 分支特有的 `present_penalty` 拼写); `max_tokens=0` = 不限制与库实现一致.
- hybrid 重置路径 (`n_tokens = 0` + `_ctx.memory_clear(True)` + `_hybrid_cache_mgr.clear()`)
  与 wheel 内部清理写法一致; 刻意用 `_model.is_hybrid()` C API 而非公开 `llm.is_hybrid`
  (含 SWA, 语义更宽) 是正确选择.
- `vram_limit=-1` 透传 `n_gpu_layers=-1` 的 auto 语义与 wheel `_parse_n_gpu_layers` 一致
  (源码确认: "auto" -> -1, "all" -> -2).
- `_estimate_n_gpu_layers` 的 `layer_size <= 0` 分支仅空文件时可达 (返回 -1 无害),
  属可留可删的防御代码, 不动.

## 其他设计权衡

- `bboxes_to_bbox` 的 999 哨兵 (返回整组) 副作用是合法索引 999 不可达、负索引下界不对称,
  tooltip 已说明, 属可接受的设计权衡; 如重构可改独立 BOOLEAN 开关.
- pyproject.toml 不设 `license` 字段 (维护者决定): 无需补充,
  不视为元数据缺口; `requires-python` 与 "llama-cpp-python 有意不列入 dependencies"
  的注释已就位.

## 测试基线

- 嵌入式 Python 实跑 `python -m unittest discover -s tests -t .` 全部通过 (当前 103 项);
  关键回归 (content 扁平化、SEG namedtuple 兼容、smart_resize 实测值、GGUF 头解析、
  显存折算边界、逐张拆分空段占位、bbox 重组对齐、handler 注册表契约) 均有覆盖.
